// (function ($) 
// {
// 	$(document).ready(function()
// 	{ 
// 		$('#Created').click(function() 
// 		{
// 			var pc_device = "win16|win32|win64|mac|macintel"; 
// 			var this_device = navigator.platform; 

// 			if ( this_device ) { 
// 				if ( pc_device.indexOf(navigator.platform.toLowerCase()) < 0 ) {
// 					$('.panel-body').load('/cli/m.create.html');
// 				} 
// 				else {
// 					$('.panel-body').load('/cli/create.html');
// 				} 
// 			}
// 			return false; 
// 		});
// 	});
	
// })(jQuery);

// (function ($) 
// {
// 	$(document).ready(function()
// 	{ 
// 		$('#KeyRestorationd').click(function() 
// 		{
// 			var pc_device = "win16|win32|win64|mac|macintel"; 
// 			var this_device = navigator.platform; 

// 			if ( this_device ) { 
// 				if ( pc_device.indexOf(navigator.platform.toLowerCase()) < 0 ) {
// 					$('.panel-body').load('/cli/m.restoration.html');
// 				} 
// 				else {
// 					$('.panel-body').load('/cli/restoration.html');
// 				} 
// 			}

// 			return false; 
// 		});
// 	});
	
// })(jQuery);

// (function ($) 
// {
// 	$(document).ready(function()
// 	{ 
// 		$('#KeyStatusd').click(function() 
// 		{

// 			var pc_device = "win16|win32|win64|mac|macintel"; 
// 			var this_device = navigator.platform; 

// 			if ( this_device ) { 
// 				if ( pc_device.indexOf(navigator.platform.toLowerCase()) < 0 ) {
// 					$('.panel-body').load('/cli/m.status.html');
// 				} 
// 				else {
// 					$('.panel-body').load('/cli/status.html');
// 				} 
// 			}
// 			return false; 
// 		});
// 	});
	
// })(jQuery);

// (function ($) 
// {
// 	$(document).ready(function()
// 	{ 
// 		$('#RegisterKeyNetworkd').click(function() 
// 		{

// 			var pc_device = "win16|win32|win64|mac|macintel"; 
// 			var this_device = navigator.platform; 

// 			if ( this_device ) { 
// 				if ( pc_device.indexOf(navigator.platform.toLowerCase()) < 0 ) {
// 					$('.panel-body').load('/cli/m.add.html');
// 				} 
// 				else {
// 					$('.panel-body').load('/cli/add.html');
// 				} 
// 			}
// 			return false; 
// 		});
// 	});
	
// })(jQuery);

(function ($) 
{
	$(document).ready(function()
	{ 

		$('#Created').click(function() 
		{
			var pc_device = "win16|win32|win64|mac|macintel"; 
			var this_device = navigator.platform; 

			if ( this_device ) { 
				if ( pc_device.indexOf(navigator.platform.toLowerCase()) < 0 ) {
					$('.panel-body').load('/cli/m.create.html');
				} 
				else {
					$('.panel-body').load('/cli/create.html');
				} 
			}
			return false; 
		});

		$('#KeyRestorationd').click(function() 
		{
			var pc_device = "win16|win32|win64|mac|macintel"; 
			var this_device = navigator.platform; 

			if ( this_device ) { 
				if ( pc_device.indexOf(navigator.platform.toLowerCase()) < 0 ) {
					$('.panel-body').load('/cli/m.restoration.html');
				} 
				else {
					$('.panel-body').load('/cli/restoration.html');
				} 
			}

			return false; 
		});

		$('#KeyRestorationd').click(function() 
		{
			var pc_device = "win16|win32|win64|mac|macintel"; 
			var this_device = navigator.platform; 

			if ( this_device ) { 
				if ( pc_device.indexOf(navigator.platform.toLowerCase()) < 0 ) {
					$('.panel-body').load('/cli/m.restoration.html');
				} 
				else {
					$('.panel-body').load('/cli/restoration.html');
				} 
			}

			return false; 
		});

		$('#KeyStatusd').click(function() 
		{

			var pc_device = "win16|win32|win64|mac|macintel"; 
			var this_device = navigator.platform; 

			if ( this_device ) { 
				if ( pc_device.indexOf(navigator.platform.toLowerCase()) < 0 ) {
					$('.panel-body').load('/cli/m.status.html');
				} 
				else {
					$('.panel-body').load('/cli/status.html');
				} 
			}
			return false; 
		});

		$('#RegisterKeyNetworkd').click(function() 
		{

			var pc_device = "win16|win32|win64|mac|macintel"; 
			var this_device = navigator.platform; 

			if ( this_device ) { 
				if ( pc_device.indexOf(navigator.platform.toLowerCase()) < 0 ) {
					$('.panel-body').load('/cli/m.add.html');
				} 
				else {
					$('.panel-body').load('/cli/add.html');
				} 
			}
			return false; 
		});

		$('#ChangeUseridd').click(function() 
		{

			var pc_device = "win16|win32|win64|mac|macintel"; 
			var this_device = navigator.platform; 

			if ( this_device ) { 
				if ( pc_device.indexOf(navigator.platform.toLowerCase()) < 0 ) {
					$('.panel-body').load('/cli/m.change_userid.html');
				} 
				else {
					$('.panel-body').load('/cli/change_userid.html');
				} 
			}
			return false; 
		});

		$('#ChangeUserkeyd').click(function() 
		{

			var pc_device = "win16|win32|win64|mac|macintel"; 
			var this_device = navigator.platform; 

			if ( this_device ) { 
				if ( pc_device.indexOf(navigator.platform.toLowerCase()) < 0 ) {
					$('.panel-body').load('/cli/m.change_userkey.html');
				} 
				else {
					$('.panel-body').load('/cli/change_userkey.html');
				} 
			}
			return false; 
		});

		$('#Transactiond').click(function() 
		{

			var pc_device = "win16|win32|win64|mac|macintel"; 
			var this_device = navigator.platform; 

			if ( this_device ) { 
				if ( pc_device.indexOf(navigator.platform.toLowerCase()) < 0 ) {
					$('.panel-body').load('/cli/m.tx.html');
				} 
				else {
					$('.panel-body').load('/cli/tx.html');
				} 
			}
			return false; 
		});
	});
	
})(jQuery);

// (function ($) 
// {
// 	$(document).ready(function()
// 	{ 
// 		$('#ChangeUserkeyd').click(function() 
// 		{

// 			var pc_device = "win16|win32|win64|mac|macintel"; 
// 			var this_device = navigator.platform; 

// 			if ( this_device ) { 
// 				if ( pc_device.indexOf(navigator.platform.toLowerCase()) < 0 ) {
// 					$('.panel-body').load('/cli/m.change_userkey.html');
// 				} 
// 				else {
// 					$('.panel-body').load('/cli/change_userkey.html');
// 				} 
// 			}
// 			return false; 
// 		});
// 	});
	
// })(jQuery);

// (function ($) 
// {
// 	$(document).ready(function()
// 	{ 
// 		$('#Transactiond').click(function() 
// 		{

// 			var pc_device = "win16|win32|win64|mac|macintel"; 
// 			var this_device = navigator.platform; 

// 			if ( this_device ) { 
// 				if ( pc_device.indexOf(navigator.platform.toLowerCase()) < 0 ) {
// 					$('.panel-body').load('/cli/m.tx.html');
// 				} 
// 				else {
// 					$('.panel-body').load('/cli/tx.html');
// 				} 
// 			}
// 			return false; 
// 		});
// 	});
	
// })(jQuery);

